{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@now/node-server",
            "config": { 
                "distDir": "dist"
             }
        }
    ]
}